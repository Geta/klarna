namespace Klarna.Common.Models
{
    public enum OrderManagementOrderStatus
    {
        AUTHORIZED,
        PART_CAPTURED,
        CAPTURED,
        CANCELLED,
        EXPIRED,
        CLOSED
    }
}
